# Shared, pinned environment spec for the whole team (PLAN.md §4.1, §5.3.1 —
# owner: Member B). Supersedes src/data/requirements.txt's provisional pins;
# everyone (A, B, C) installs from this file so training/eval/preprocessing
# all run against the same library versions.
torch>=2.2
torchvision>=0.17
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8
matplotlib>=3.7
PyYAML>=6.0
tabulate>=0.9   # DataFrame.to_markdown(), used by src/data and src/eval reports
