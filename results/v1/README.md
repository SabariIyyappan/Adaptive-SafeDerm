# results/v1/ — Member A's V1 outputs (PLAN.md §5.3, §5.6)

## Status

| Item | Status |
|---|---|
| `architecture_figure.png` | **Done** — no dataset needed, generated by `src/data/architecture_figure.py` |
| Integrity audit, split, split verification, EDA figures, dataset summary | **Code done + unit-tested** against a synthetic fixture (`tests/`), **not yet run on real data** — this environment has no network route to Kaggle/Dataverse. Run the commands in `data/README.md` once real HAM10000 is acquired. |
| V1 results deck (6–8 slides) | Outline drafted in `deck_outline.md` for A's slides (dataset / split / architecture). Waiting on C's evaluation figures per PLAN.md §5.4 before it can be finished and exported as a PDF backup. |

## Reproducing once real data is present

```bash
pip install -r src/data/requirements.txt
python -m src.data.acquire --dest data/raw --source kaggle       # or --source dataverse
python -m src.data.integrity_audit --data-root data/raw --out results/v1/integrity_audit.md
python -m src.data.split --data-root data/raw --out data/splits --seed 42
python -m src.data.verify_split --splits data/splits --data-root data/raw --out results/v1/split_verification.md
python -m src.data.eda --data-root data/raw --out results/v1/eda
python -m src.data.cache_resize --data-root data/raw --out data/cache_raw224   # optional speed-up for B
```

Every one of these commands was exercised end-to-end against a small synthetic fixture during development (`tests/test_data_pipeline.py`, `python3 -m unittest discover -s tests -v` — 16/16 passing) to confirm the split has zero lesion/image leakage, covers every image exactly once, and has every class present in every split, before being pointed at real data.

## "Done when" (PLAN.md §5.3, Member A)

- [x] Integrity audit, split, split verification and EDA logic implemented and unit-tested.
- [ ] Split files pass every verification check **against the real 10,015-image dataset** — blocked on data acquisition (see `data/README.md`).
- [ ] B and C have confirmed they can load the real split files.
- [ ] V1 deck finished with C's figures and exported as a PDF backup.
