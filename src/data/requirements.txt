# Provisional — scoped to Member A's src/data/ scripts only.
# PLAN.md §4.1 assigns env/ (the shared, pinned environment spec for the
# whole team) to Member B; once B publishes it, this file should be
# reconciled with / superseded by it (see data/README.md).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
Pillow>=10.0
matplotlib>=3.7
tabulate>=0.9   # needed for DataFrame.to_markdown()
kaggle>=1.6     # only needed to run acquire.py --source kaggle

# Test suite uses Python's stdlib unittest (no pytest dependency), so it
# runs in network-restricted environments too:
#   python3 -m unittest discover -s tests -v
